import { NewPasswordForm } from "@/components/NewPasswordForm";

export const metadata = {
  title: "Nouveau mot de passe — Nova Compagnie",
  robots: { index: false },
};

export default function NewPasswordPage() {
  return <NewPasswordForm />;
}
